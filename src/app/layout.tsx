import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mike ONeal | AI Augmented Software Engineer",
  description: "I don't hold titles or put time in. I get shit done. Design, plan, engineer, test, deploy, audit — Systems, Mobile, Web, Desktop, Cloud.",
  openGraph: {
    title: "Mike ONeal | AI Augmented Software Engineer",
    description: "I don't hold titles or put time in. I get shit done.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-black text-white`}
      >
        {children}
      </body>
    </html>
  );
}
